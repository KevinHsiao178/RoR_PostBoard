<%= form_for([@article, @article.comments.build]) do |f| %>
  <%= f.label :body, 'Comment:' %><br>
  <%= f.text_area :body, class: "form-control", placeholder: "Leave comments..." %>
  <br>
  <%= f.submit class: "btn btn-success" %>
<% end %>